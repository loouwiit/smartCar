#pragma once
#include <stddef.h>
bool prefixCompare(const char* string, unsigned stringLength, const char* prefix, unsigned prefixLength);
bool stringCompare(const char* string, unsigned stringLength, const char* standard, unsigned standardLength);
